# 截图 / 演示资源

把项目演示截图或 GIF 命名为 `screenshot.png`（或 `.gif`）放在本目录，
然后修改根目录 README 顶部那张图的 `src` 指向 `docs/screenshot.png`。

录制建议（10~15 秒一镜到底，最能打动人）：
1. 输入问题 → 流式逐字输出
2. 问「北京天气」→ 触发工具调用，时间线显示耗时
3. 让它「画一张/找一张图」→ 出图
4. 点 ⚙️ 切换一个供应商 → 继续对话
